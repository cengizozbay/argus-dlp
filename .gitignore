# ---- Derleme ciktilari ----
**/bin/
**/obj/

# ---- Veritabani / veri ----
*.db
*.db-wal
*.db-shm

# ---- SIRLAR - ASLA commit etme! ----
# tarayici uzantisi imzalama anahtari (ID buna bagli)
deploy/argus-ext.pem
argus-ext.pem
# HTTPS sertifikalari
*.pfx
# gercek baglanti dizeleri / sifreler
appsettings.Production.json

# ---- Uretilen dagitim artefaktlari (buyuk binler - build uretir, repoya girmez) ----
backend/wwwroot/download/*.zip
extension.crx
extension.pem
# NOT: backend/wwwroot/ext/ (CRX/XPI/extid ~2KB) sunucunun eklentiyi dagitmasi icin repoda DURUR
# (git pull ile sunucuya gitsin). Imza anahtari deploy/argus-ext.pem gizli kalir.

# ---- Log / gecici ----
logs/
*.log
backups/

# Test çıktıları
tests/bin/
tests/obj/
